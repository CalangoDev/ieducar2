<?php
if (count($this->dados) > 0):
    $estilos = array('active','warning');
    $this->cycle()->assign($estilos);
    foreach ($this->dados as $formulaMedia):
        ?>
        <tr class="<?php echo $this->cycle()->next();?>">
            <td>
                <?php
                echo $formulaMedia->getId();
                ?>
            </td>
            <td>
                <?php
                echo $formulaMedia->getNome();
                ?>
            </td>
            <td>
                <?php echo $formulaMedia->getFormulaMedia();?>
            </td>
            <td>
                <?php echo ($formulaMedia->getTipoFormula() == 1) ? 'Média Final' :
                    'Média para Recuperação';?>
            </td>
            <td>
                <a href="#<?php echo $formulaMedia->getId()?>"
                   class="btn btn-info btn-detalhes" data-toggle="modal"
                   data-target="#detalhesModal"><i class="icon icon-list"></i> Ver detalhes</a>
                <a href="/escola/formula-media/save/id/<?php echo $formulaMedia->getId()?>"
                   class="btn btn-primary"><i class="icon icon-pencil"></i> Editar</a>
                <a href="/escola/formula-media/delete/id/<?php echo $formulaMedia->getId()?>"
                   class="btn btn-danger delete"><i class="icon icon-trash"></i> Apagar</a>
            </td>
        </tr>
        <?php
    endforeach;
else:
    ?>
    <tr class="warning">
        <td colspan="5" class="text-center">
            <b>Nenhum registro encontrado</b>
        </td>
    </tr>
    <?php
endif;
?>