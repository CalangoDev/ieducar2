<?php
if (count($this->dados) > 0):
    $estilos = array('active','warning');
    $this->cycle()->assign($estilos);
    foreach ($this->dados as $tabela):
        ?>
        <tr class="<?php echo $this->cycle()->next();?>">
            <td>
                <?php
                echo $tabela->getId();
                ?>
            </td>
            <td>
                <?php
                echo $tabela->getNome();
                ?>
            </td>
            <td>
                <?php echo ($tabela->getTipoNota() == 1) ? 'Nota númerica' :
                    'Nota conceitual';?>
            </td>
            <td>
                <a href="#<?php echo $tabela->getId()?>"
                   class="btn btn-info btn-detalhes" data-toggle="modal"
                   data-target="#detalhesModal"><i class="icon icon-list"></i> Ver detalhes</a>
                <a href="/escola/tabela-arredondamento/save/id/<?php echo $tabela->getId()?>"
                   class="btn btn-primary"><i class="icon icon-pencil"></i> Editar</a>
                <a href="/escola/tabela-arredondamento/delete/id/<?php echo $tabela->getId()?>"
                   class="btn btn-danger delete"><i class="icon icon-trash"></i> Apagar</a>
            </td>
        </tr>
        <?php
    endforeach;
else:
    ?>
    <tr class="warning">
        <td colspan="5" class="text-center">
            <b>Nenhum registro encontrado</b>
        </td>
    </tr>
    <?php
endif;
?>