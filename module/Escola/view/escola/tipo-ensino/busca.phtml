<?php
if (count($this->dados) > 0):
    $estilos = array('active','warning');
    $this->cycle()->assign($estilos);
    foreach ($this->dados as $tipoEnsino):
        ?>
        <tr class="<?php echo $this->cycle()->next();?>">
            <td>
                <?php
                echo $tipoEnsino->getId();
                ?>
            </td>
            <td>
                <?php
                echo $tipoEnsino->getNome();
                ?>
            </td>
            <td>
                <?php
                echo $tipoEnsino->getInstituicao()->getNome();
                ?>
            </td>
            <td>
                <?php
                if ($tipoEnsino->getAtivo()){
                    $situacao = 'Sim';
                    $label = 'success';
                } else {
                    $situacao = 'Não';
                    $label = 'default';
                }
                ?>
                <span class="label label-<?php echo $label;?>"><?php echo $situacao;?></span>
            </td>
            <td>
                <a href="#<?php echo $tipoEnsino->getId()?>"
                   class="btn btn-info btn-detalhes" data-toggle="modal"
                   data-target="#detalhesModal"><i class="icon icon-list"></i> Ver detalhes</a>
                <a href="/escola/tipo-ensino/save/id/<?php echo $tipoEnsino->getId()?>" class="btn btn-primary"><i class="icon icon-pencil"></i> Editar</a>
                <a href="/escola/tipo-ensino/delete/id/<?php echo $tipoEnsino->getId()?>" class="btn btn-danger delete"><i class="icon icon-trash"></i> Apagar</a>
            </td>
        </tr>
        <?php
    endforeach;
else:
    ?>
    <tr class="warning">
        <td colspan="5" class="text-center">
            <b>Nenhum registro encontrado</b>
        </td>
    </tr>
    <?php
endif;
?>